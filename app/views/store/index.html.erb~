<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h1>Your APP Catalog</h1>

<% @apps.each do |app| %>
  <div class="entry">
    <%= image_tag(app.image_irl) %>
    <h3><%= app.name %></h3>
    <%= sanitize app.developer %>
    <%= sanitize app.description %>
    <div class="price_line">
      <span class="price"><%= app.price %></span> 
    </div>
    <div class="ranting_line">
      <span class="ranting"><%= app.ranting %></span> 
    </div>
  </div>
<% end %>
